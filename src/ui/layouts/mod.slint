import { VerticalBox } from "std-widgets.slint";
import { Colors, Fonts, Styles } from "../themes/mod.slint";

component TextLabel inherits Text {
    color: Colors.text;
    font-size: Fonts.size-normal;
}

// 卡片布局
export component Card {
    in property <string> title;
    in property <brush> background: Colors.surface;
    in property <length> card-padding: Styles.spacing-normal;

    Rectangle {
        background: root.background;
        border-radius: Styles.radius-normal;
        drop-shadow-color: Styles.shadow-normal;
        padding: root.card-padding;

        VerticalBox {
            spacing: Styles.spacing-normal;

            if root.title != "" : TextLabel {
                text: root.title;
                font-size: Fonts.size-large;
                font-weight: Fonts.weight-medium;
            }

            @children
        }
    }
}

// 工具布局
export component ToolLayout {
    in property <string> title;
    in property <string> description;

    Card {
        title: root.title;

        VerticalBox {
            spacing: Styles.spacing-normal;

            if root.description != "" : TextLabel {
                text: root.description;
                color: Colors.text-secondary;
            }

            @children
        }
    }
}

// 分割线
export component Divider {
    in property <brush> color: Colors.divider;
    in property <length> thickness: 1px;

    Rectangle {
        height: root.thickness;
        background: root.color;
    }
} 